server:
  port: 8082
spring:
  application:
    name: consumer-service
eureka:
  client:
    service-url: # EurekaServer的地址，现在是自己的地址，如果是集群，需要加上其它Server的地址。
      defaultZone: http://127.0.0.1:10086/eureka
    registry-fetch-interval-seconds: 30 #拉取间隔时间 默认30